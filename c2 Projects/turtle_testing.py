import turtle
Bob = turtle.Turtle()
for i in range(999):
    Bob.fd(50)
    Bob.rt(999)
    Bob.fd(999)
    Bob.lt(999)
screen = turtle.Screen()
screen.setup(width=0.56, height=0.92, startx=0, starty=0)